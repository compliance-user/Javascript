// const fruits = new Array("Hello", 2, 3); // Creating array with Array object
// console.log(fruits[0]) // Index

const newArr = ['Hello', 2.5, null, 1, {'name': 'shiva'}];
// console.log(newArr.length)

// newArr.push('add'); // equivalent to append in python
// console.log(newArr);
// newArr.unshift('minues'); // equivalent to insert in python
// console.log(newArr);

// newArr.pop('Hello'); // Identical functionality as python
// console.log(popped);

// idx = newArr.indexOf(2.5);

// console.log(idx);

// concat of values

// const val = 10;

// const cc = newArr.concat(val);
// console.log(cc)

// adding new array of values into an existing array

// const arr1 = [1, 2, 3];
// const arr2 = [4, 5, 6];
// const do_concat = arr1.concat(arr2); // equivalent of extend in python
// console.log(do_concat);

